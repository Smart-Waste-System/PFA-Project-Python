version: '3.8'

services:
  postgres_py:
    image: postgis/postgis:latest
    container_name: smart_waste_db_python
    environment:
      POSTGRES_USER: aybex_py
      POSTGRES_PASSWORD: password_py
      POSTGRES_DB: smart_waste_py
    ports:
      - "5433:5432"
    volumes:
      - pgdata_py:/var/lib/postgresql/data
    restart: always

  redis_py:
    image: redis:alpine
    container_name: smart_waste_redis_python
    ports:
      - "6380:6379"
    restart: always

  mosquitto_py:
    image: eclipse-mosquitto:1.6
    container_name: smart_waste_mqtt_python
    ports:
      - "1884:1883" # Port MQTT pour Python
      - "9002:9001" # Port WebSockets pour Python
    volumes:
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    restart: always

volumes:
  pgdata_py: